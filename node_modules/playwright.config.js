import { defineConfig } from '@playwright/test';

export default defineConfig({
  testDir: './tests', // Укажи папку, где находятся тесты
  testMatch: '**/*.spec.js', // Поиск файлов, соответствующих этому паттерну
  reporter: 'html', // Добавит HTML-отчёт
  use: {
    headless: true, // Запуск браузера в фоновом режиме
    screenshot: 'only-on-failure', // Делает скриншоты при падении теста
    video: 'retain-on-failure' // Записывает видео, если тест упал
  },
});

module.exports = {
    testDir: './tests',
    testMatch: '**/*.spec.js',
    reporter: 'html',
    use: {
      headless: true,
      screenshot: 'only-on-failure',
      video: 'retain-on-failure'
    },
  };